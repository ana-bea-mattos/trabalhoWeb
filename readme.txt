Criei a pasta "trabweb", em seguida abri o git bash e localizei o arquivo (cd.. cd.. cd trabweb), depois o git init para inicializar.
Na pasta (trabweb), criei os arquvios index.html e projeto.js, e com o git add . adicionei eles a fila de espera, com um git commit -m "0.1 -arquvio vazio" foi commitado a versão. 
depois adicionei os registros no arquivo index.html e fiz o mesmo processo (git add ., git commit -m "0.2 - index"). o mesmo processo para o arquivo projeto.js (git add ., git commit -m "0.3 - js"). sempre utilizando do comando "git log" para saber se o commit foi commitado.
